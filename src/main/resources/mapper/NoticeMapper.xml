<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlut.ResearchService.mapper.NoticeMapper">


    <select id="selectPageNotice" resultType="java.lang.String">
        SELECT title FROM RDService.notice WHERE target_page = #{page}
    </select>
</mapper>